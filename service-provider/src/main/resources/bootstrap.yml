server:
  port: 8081

spring:
  application:
    name: service-provider
  cloud:
    consul:
      host: 47.101.189.119
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${server.port}
