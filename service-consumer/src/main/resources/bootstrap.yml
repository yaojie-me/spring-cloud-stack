server:
  port: 8082

spring:
  application:
    name: service-consumer
  cloud:
    consul:
      host: 47.101.189.119
      port: 8500
      discovery:
        instance-id: ${spring.application.name}-${server.port}
